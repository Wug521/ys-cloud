spring:
  application:
    name: Ys-Gateway
  cloud:
    nacos:
      discovery:
        server-addr: ${nacos.url}:8848
      config:
        server-addr: ${nacos.url}:8848
        group: DEFAULT_GROUP
        prefix: ys-gateway
        file-extension: yaml