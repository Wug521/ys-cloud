spring:
  application:
    name: Ys-Auth
  cloud:
    nacos:
      discovery:
        server-addr: ${nacos.url}:8848
      config:
        server-addr: ${nacos.url}:8848
        group: DEFAULT_GROUP
        prefix: ys-auth
        file-extension: yaml