spring:
  application:
    name: YS-Monitor-Admin
  cloud:
    nacos:
      config:
        server-addr: ${nacos.url}:8848
        group: DEFAULT_GROUP
        prefix: ys-monitor-admin
        file-extension: yaml